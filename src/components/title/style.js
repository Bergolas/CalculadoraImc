import {StyleSheet } from "react-native";


const styles = StyleSheet.create({
    boxTitle: {
        alignItems: 'center',
        marginTop: 50,
        padding: 10,
    },
    titleText: {
        color: "#000000",
        fontWeight: 'bold',
        fontSize: 30,
    }
});

export default styles;